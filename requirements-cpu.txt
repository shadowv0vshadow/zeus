# YouDub CPU版本依赖配置（适用于无GPU的Linux服务器）
# 使用方式: pip install -r requirements-cpu.txt

# 基础依赖
gradio==5.49.1
loguru==0.7.3
yt-dlp==2025.10.22
scipy==1.15.3
librosa==0.10.0
python-dotenv==1.1.1
openai==2.6.1
dashscope==1.24.9
audiostretchy==1.3.5
bilibili-toolman==1.0.7.9
numpy<2.0,>=1.21.0
websockets>=11.0,<13.0  # gradio 5.49.1 需要兼容的 websockets 版本
gradio-client<1.13.0,>=1.10.0  # 兼容 websockets 12.0

# PyTorch CPU版本（必须首先安装）
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1
torchvision
torchaudio==2.0.2

# Demucs（从git安装，必须在PyTorch之后）
git+https://github.com/facebookresearch/demucs#egg=demucs

# WhisperX（从git安装）
git+https://github.com/m-bain/whisperx.git

# TTS（可选，如果需要XTTS模型）
# TTS

# 其他依赖（会自动安装）
# pytorch-lightning==2.0.9
# lightning==2.0.9
# pyannote.audio==2.0.1

